import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  buttonContainer: {
    backgroundColor: '#f90',
    paddingVertical: 15,
    borderRadius: 20,
  },
  buttonText: {
    color: '#fff',
    textAlign: 'center',
    fontWeight: '700',
  },
});

export default styles;
